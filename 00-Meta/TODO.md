# Short-Term Plan
### 프로젝트
- [x] 쿼리 최적화 인덱스 flyway 적용
- [ ] 브라우저 캐시 진행
- [ ] 배치 + 스케쥴러 처리 및 서버 분리 -> 멀티모듈이 아니더라도 할 수 있는 방법은..? 그냥 배포하면 되긴 함.
- [ ] 알림 비동기 설계 -> 알림 처리도 따로 서버 분리? Redis Pub/Sub을 사용?
- [ ] 각 기술마다 버전 별 특징을 알고 있어야 할듯. + 각 언어 마다 특징 차이점
- [x] 검색 인덱스 -> pg_trgm 적용 -> 쿼리도 어떻게 적용할지 알고 있으면 좋을듯? + redis 실시간 인기 검색어도 구현함. -> 이력서 추가해야함
- [ ] 실패 -> retry + 서킷 브레이커?
- [ ] 백업 db, 이중화 등
### 이론 정리
- [ ] 자바 컬렉션 계열 동시성 지원 여부 분석 - 너무 깊이 팔 필요는 없다. 중요한건 본질. 원자성 연산의 지원 및 효율성
- [x] git 고급 명령어들 노트 정리 및 안키
### AI 활용 공부

- [ ] AI - 설계중...
### 구인
- [ ] 이력서 매칭 AI
- [ ] 면접 연습 AI - 면접은 내가 모르는 내용이 나올 수 밖에 없는 구조이며, 이를 대하는 태도를 제일 중요시 본다. 어떻게 지혜롭게 해쳐나갈 것인가
- [ ] 면접 - 공백기 질문 대비
### sqld
- [ ] sqld 자격증 신청함. 공부해야함.
# Long-Term Plan
- AWS SAA 자격증
- 로그 알림 설계
- 프로젝트 - 쿼리 최적화, 역정규화, 동시성 이슈 해결, 배치 처리, 데이터 쿼리 분석 및 통계 처리, 로깅 데이터 비동기 동시성 및 장애 대응, 프론트 연동 및 시연
- Claude Code 유출 자료 본질 1개씩 분석 [[정리-Claude_Code_내부_로직]]
- AI - 프로젝트 구조를 최대한 토큰을 덜 먹게 구성하는건 어떰?

# Temp
### batch


### observerability


### cache
- cache evict race condition 상황 - DB commit 안된 상태에서 동시에 다른 트랜잭션이 조회 요청을 하고, 미리 tx 전에 cache evict되어 DB에서 구버전 데이터를 조회해서 캐시에 저장함 -> 정합성 문제 발생. cache가 이전 버전의 데이터를 가리킴.
- TransactionAwareCacheManagerProxy를 통해 evict가 commit 후로 미뤄짐 + 실패 시 commit이 아닌 rollback되었으므로 캐시를 비우지 않음 -> spring에서 제공하는거겠지?
- 하지만 이 경우 Cache Stampede 현상 발생할 수 있음. Evict는 캐시를 단순히 비우는 작업이라서 비운 이후에 동시에 조회가 1000건이 발생하면 DB Connection 부족으로 예외를 발생시킬 수 있음. 
- write-through는 rollback을 직접 구현해야 되므로 캐시 저장소와 서버가 분리되어 있는 한 원자성 문제 발생
- @CachePut도 업데이트를 하긴 하나, commit 타이밍이 잘못되면 똑같이 Stampede 현상 발생할 수 있음. 
- Redis로 글로벌캐시를 사용해서 관리 포인트 줄이고, 노드 간 캐시 불일치를 완화할 수 있음. 하지만 DB와 캐시 사이의 dual write consistency를 막을 수는 없다.

### GC
- stop-and-wait
- G1 GC -> ZGC 이점은?
- GC 매트릭 보는법
	- heap이 얼마나 차는지
	- GC pause가 튀는지
	- 요청 지여 시간과 GC가 같이 움직이는지
	- CPU가 같이 치솟는지
	- thread 수가 이상하게 늘어나는지

Q. jvm heap metric이 used가 위아래로 왔다갔다함. GC의 영향임? 하지만 일정 구간을 반복으로 3MB씩 선형적으로 증가하긴 함.
committed와 max는 어떤 의미임?

Q. garbage collection end of minor GC는 뭐임?(G1 Evacuation Pause) 0.025 수치는 뭘까. 

### 변경 이력이 중요한 데이터
- 차량 대절 상태 변경 이력 -> append-only/event 형태로 관리 -> 실무에서 많이 나오는 패턴
- 왜 등장? -> 어떻게 그 상태가 되었는가? -> 갖가지 운영, 비즈니스, CS, 분쟁, 장애 복구 관련 질문들이 등장함
	- 왜 취소됐지? 누가 변경했지? 언제? 이전 상태는 뭐였지?
	- 매칭 완료까지 평균 몇 분 걸리지? 어느 당계에서 가장 많이 이탈하지? -> 데이터 분석용
	- 사용자는 승인했다고 하는데?, 주최자가 먼저 취소했나?
	- 이 상태가 왜 꼬였지? 이벤트 재생하면 복구 가능한가?
	이 질문들에 대해 답변을 할 수 없음
- log가 있지 않나?
	- log만으로는 지금 데이터 모델에 통합이 어려움. -> 스키마 구성이 약함
	- 일반 로그와 audit log의 구분 이해 필요.
		- 에러가 났나?
		- 어디서 느려졌나?
		- 요청이 어떤 흐름을 탔나?
	- 반면 audit log는
		- 누가
		- 언제
		- 무엇을
		- 왜/어떤 이유로
		- 이전 값에서 어떤 값으로 변경했는가
	- 보존 기간이 짧음
	- 비즈니스 데이터와 연결이 약함. 
	- 정합성 문제(log는 주로 비동기로 전송함)
- event 테이블로 관리?
	- 이벤트(혹은 append-only) + 현재 상태 두개 동시에 관리
	- join의 부담 덜어줌 + 이벤트 write를 트랜잭션 포함 => 원자성 확보
- 인증/보안 도메인에서도
	- 로그인 성공
	- 비밀번호 변경
	- 2차인증 실패
	- 권한 변경
	등의 감사 로그가 필요함. 법적으로 의미 있는 것들 위주
	즉 이력 데이터(로그성 데이터)이자 나중에 관리 대상이 되는 것들. (반면 임시 데이터는 "지금 처리 끝나면 의미가 사라짐", "미래에 추적,보존,재현 필요 없음")