# 플랜

- [x] **Allreva 리팩토링**: `Concert` 도메인의 `jsonb` 매핑 에러 해결 (JPA `Property is mapped as basic aggregate component array` 오류 분석 및 수정)
- [ ] **동시성 딥다이브**: DB 레벨 락(S-Lock, X-Lock, 의도 락) 심화 학습 및 [[동시성지도.canvas]] 업데이트
- [ ] **Anki 복습**: 최근 추가된 락(Lock) 메커니즘 및 동시성 이상 현상(7가지) 카드 암기
- [ ] **정처기 실기**: 이론 파트(키워드 중심) 일일 할당량 공부 및 카드 생성
- [ ] **홈 서버**: 노트북 홈 서버 구축을 위한 온프레미스 환경 조사 및 초기 세팅 계획

# 기록

### Allreva 기술 부채 해결
- Concert 도메인 jsonb 적용 중 발생한 JPA 에러는 컬렉션 매핑 방식의 문제로 보임. `AttributeConverter` 또는 `Hypersistence Optimizer` 라이브러리 검토 필요.

### 동시성 제어 전략
- Application 락을 넘어 DB 엔진 수준에서의 격리 수준과 락의 상관관계를 면접 답변용(30초)으로 정리할 것.

### json converter 제작
[[사례-Hibernate Embedded 컬렉션의 JSON 매핑 실패와 한계]]

### 리팩토링 거의 끝남.
프론트도 같이 시작해야할듯. AI 도움 받으면 금방 할 것 같음. 대신 이건 private로 만들어야 될 듯.
ci cd쪽도 조금만 건드리면 될 듯 하다.
test application.yml 건들기 필요.
테스트 시 외부 port 테스트를 어떻게 설계할지 고민.
s3는 테스트 가능?

### kopis 데이터 가져오기 기능

### spotify 가져오기

